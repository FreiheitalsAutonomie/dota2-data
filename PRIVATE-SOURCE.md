# 原站与镜像访问

原站由站点所有者使用 ChatGPT 账号登录，其他访客通过 GitHub Pages 镜像查看展示数据。

镜像的 `Sync and publish scrim mirror` 工作流每小时自动同步，也支持在 Actions 页面手动 Run workflow。只有完整数据校验和发布均成功，镜像才更新；失败时继续保留上一次成功版本。

仓库 Actions Secret `SITES_SYNC_TOKEN` 保存 Sites 提供的接口访问凭证。它只在抓取快照步骤中注入，并通过 `OAI-Sites-Authorization` 请求头发送到原站的固定导出接口。不要把凭证放入代码、网页、公开文件、日志或工作流参数中。凭证是敏感的站点 API 访问凭证，不是选手或管理员的登录账号。

若平台轮换凭证，应同步更新此 Secret，再运行工作流验证。不要通过重新开放原站来修复同步失败。工作流成功后，可检查镜像顶部的数据同步时间。

镜像仍为公开站点；原站的管理员访问限制不改变镜像的公开范围。
